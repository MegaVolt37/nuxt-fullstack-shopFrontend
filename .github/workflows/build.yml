name: Build Project
on:
  workflow_dispatch:
  pull_request:
    types: [opened, edited, reopened]
jobs:
  building:
    runs-on: windows-latest
    steps:
      - name: Get repository
        uses: actions/checkout@v3
      - name: npm install
        run: npm ci
      - name: Build
        run: npm run build